{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "C++ Launch Game",
            // FIX 1: Change type to cppvsdbg (Native Windows VS Debugger)
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/Game.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}/build",
            "environment": [],
            "externalConsole": false,
            // REMOVED: MIMode and miDebuggerPath lines are deleted
            "preLaunchTask": "C/C++: cl.exe build active file"
        }
    ]
}